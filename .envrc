# Make scripts/ available on PATH so setup.sh (and any future scripts) are
# runnable directly without a path prefix: e.g. `setup.sh` instead of `./scripts/setup.sh`
PATH_add scripts
PATH_add ralph

# https://docs.dagster.io/getting-started/installation
# venv enables access to: $ dg --version
source .venv/bin/activate
