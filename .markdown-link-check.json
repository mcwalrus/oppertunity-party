{
  "baseUrl": "https://www.opportunity.org.nz",
  "timeout": "10s",
  "retryOn429": true,
  "retryCount": 2,
  "fallbackRetryDelay": "5s",
  "aliveStatusCodes": [200, 206],
  "ignorePatterns": [
    { "pattern": "^#",      "comment": "Bare fragment anchors" },
    { "pattern": "^mailto:", "comment": "mailto links" },
    { "pattern": "^/",      "comment": "Root-relative paths (NationBuilder navigation) cannot be resolved from a local file; absolute URLs in the scraped metadata header are checked instead" }
  ],
  "httpHeaders": [
    {
      "urls": ["https://", "http://", "/"],
      "comment": "Apply a browser-like UA to all requests so NationBuilder doesn't 400 them",
      "headers": {
        "User-Agent": "Mozilla/5.0 (compatible; markdown-link-check/3)"
      }
    }
  ]
}
